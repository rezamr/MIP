export const SettingsPage = Object.freeze({
  id: "settings",
  title: "Settings & Data",
  description: "Manage owner preferences, diagnostics, backups, restore, import, and export.",
  handsFree: false,
});

export default SettingsPage;
