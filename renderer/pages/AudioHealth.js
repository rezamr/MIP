export const AudioHealthPage = Object.freeze({
  id: "health",
  title: "Audio Health Check",
  description: "Capture real AudioWorklet telemetry and persist the owner’s audible result separately.",
  handsFree: false,
});

export default AudioHealthPage;
