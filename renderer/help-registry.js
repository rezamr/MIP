// Offline, page-aware owner guidance.  This registry is deliberately data-only
// so the renderer can render the same content in development and packaged
// Electron builds without an HTTP/help dependency.
const page = (title, purpose, doing, doNow, avoid, values, statuses, boundary, test) => Object.freeze({ title, purpose, doing, doNow, avoid, values, statuses, boundary, test });

export const NAVIGATION_PAGE_IDS = Object.freeze([
  "start", "audio", "profiles", "recipes", "calibration", "health", "reports", "aggregate", "settings",
]);

export const PAGE_HELP = Object.freeze({
  start: page(
    "Start Research Session",
    "Commit and execute one declared research session from immutable profile and recipe versions.",
    "The Electron main process owns profile resolution, target assignment, commitment, AudioWorklet authentication, named-event timing, evidence persistence, and reveal gating.",
    "Select a profile, review outcome space/cardinality, RNG, cadence, target anchor, windows, endpoint, recipe, and reveal policy. Confirm baseline/safety, memory/readiness, commit, wait for PROCESSOR_READY, then start. Return ends the participant phase; it does not necessarily end evidence monitoring.",
    "Do not alter profile/recipe versions, target semantics, timing, endpoint, or reveal policy after commitment. Never infer hidden objective data from a pre-reveal screen.",
    "Participant phase is owner-facing; evidence phase may continue. Raw report draft is mutable; lock is immutable. Reveal eligibility is separate from report lock.",
    "PASS means authenticated audio and protocol events are persisted. WARNING means a timing/recovery deviation or evidence phase is incomplete. FAILURE requires explicit recovery and no backfill.",
    "A session result is not proof of a mechanism or historical audio. Integrity and protocol status precede statistical interpretation.",
    "Run a dry session, inspect PROCESSOR_READY, stop/finalize, return, lock a report, verify the gate, integrity, and export.",
  ),
  audio: page(
    "Audio Lab",
    "Explore and verify live audio recipes; this workspace is not automatically formal evidence.",
    "Audio Lab, offline fixtures, and formal sessions share normalized AudioEngine semantics. AudioWorklet telemetry authenticates generated digital PCM; OS/device acoustics remain outside the digest.",
    "Select a repository-backed recipe, inspect active layers/source provenance/engineering verification, then Play, Pause, Resume, Stop, or adjust preview Master gain. Record any audible observation in Audio Health.",
    "Do not treat a pleasant sound as historical proof, add noise to mask hiss, or assume a layer is active because the engine supports it. Preview gain never rewrites a committed session.",
    "Component gain, noise gain, effect mix, envelope, headroom, Master gain, and OS volume are different stages. Stop ramps to zero, finalizes, disconnects, and closes the context.",
    "PASS is a software fixture/telemetry result. Owner observations are Clean, Artifact heard, Left-right issue, or Uncertain and remain separate.",
    "A correct PCM digest cannot prove Bluetooth/wired/DAC/headphone output or the waveform at the eardrum.",
    "Audition A-U396-4, A-P100-104, or A-SHAM-0, change Master gain, Pause, Resume, Stop, and confirm generated frames no longer increase.",
  ),
  profiles: page(
    "Experiment Profiles",
    "Manage immutable experiment/protocol definitions and versions.",
    "SQLite stores drafts, validation, config hashes, activation, and parent versions. Profiles define mode, outcome space, RNG, timing, windows, endpoint, recipe reference, and reveal policy.",
    "Duplicate/Edit Draft, Validate, Diff, Save New Version, then Activate after review. Existing sessions retain their original profile version.",
    "Never edit a committed version in place or assume an outcome-range edit changes audio without selecting another recipe.",
    "Version, status, active flag, config fingerprint, outcome cardinality, and recipe reference identify the effective protocol.",
    "PASS means validation and immutable persistence succeeded. DRAFT/INACTIVE/UNKNOWN require review before use.",
    "A valid protocol is not evidence that a scientific hypothesis is true.",
    "Duplicate the baseline, change one window, validate/diff/save, and confirm the old version is unchanged.",
  ),
  recipes: page(
    "Audio Recipe Library",
    "Inspect and version immutable synthesis definitions with component-level provenance.",
    "SQLite resolves normalized recipes, fingerprints, active-layer projections, provenance entries, and software verification summaries. Repository-owned built-ins reconcile metadata only when the persisted material fingerprint matches the current canonical definition.",
    "Open a recipe card, read Source & Provenance and Engineering verification, inspect active layers, audition only active complete versions, and save edits as new versions. Custom versions explicitly show: No golden reference fixture applies to this custom recipe.",
    "Do not call a reconstruction historically exact or treat engineering PASS as source verification. UNKNOWN_BLOCKED material cannot be formally activated. A material fingerprint mismatch remains immutable and requires review.",
    "Carrier frequencies/gains/phases, noise/effects/modulation, execution mode, fingerprint, provenance class, activation, formal operational eligibility, reference status, and historical badge are distinct values.",
    "PASS is software verification only where an applicable reference fixture matches. A custom recipe may be operationally eligible with reference status NOT_APPLICABLE; MIP DEFINED, DOCUMENTED PATENT COMPARATOR, PATENT-ARCHITECTURE RECONSTRUCTION, and SHAM CONTROL are explicit badges.",
    "No current built-in recipe is a blanket historically exact CENTER LANE waveform; `base` semantics remain unresolved.",
    "Open MIP_LAYERED_EXPERIMENTAL_V1 and verify repository/version backing, active layers, mixed provenance, and fingerprint.",
  ),
  calibration: page(
    "Calibration",
    "Measure the configured random provider independently of a participant session.",
    "The main-process RNG provider records samples, the selected normalized outcome space and cardinality K, provider version, and an immutable result hash. The selector can use the application research default, a profile space, binary, or an explicit integer range.",
    "Choose the outcome-space source before running. For a sparse large range, enter inclusive minimum/maximum bounds and review the calculated K; run OS_CSPRNG before operational testing and use deterministic provider only for labelled fixtures.",
    "Calibration does not make an RNG more random, prove paranormal effects, or enumerate a billion-value outcome space. Range bounds are symbolic and are never expanded into a billion-item list.",
    "Provider, sample count, outcome space, cardinality K, unique/duplicate outcomes, observed numeric min/max, bucket summary, result hash, and integrity status are diagnostics.",
    "VERIFIED means the persisted calibration hash checks; it is not a scientific result.",
    "Provider diagnostics do not validate a participant outcome or an audio recipe.",
    "Run a small OS-CSPRNG calibration and verify its immutable detail/hash.",
  ),
  health: page(
    "Audio Health Check",
    "Check the digital AudioWorklet path and record a separate physical listening observation.",
    "A main-process challenge authenticates processor identity, recipe/version, nonce, sample rate, canonical PCM digest, continuity, clipping, context state, and latency.",
    "Run 60-second quick check; run 10-minute stability and 60-minute owner soak only when explicitly authorized. Record Clean/Artifact heard/Left-right issue/Uncertain without changing the recipe.",
    "Do not equate a digest with analog acoustics or auto-tune a recipe based on owner preference.",
    "Telemetry frames/digest/continuity/clipping/context/latency are software; owner result is physical observation.",
    "VERIFIED means challenge and digital telemetry match. An owner artifact is still a valid observation.",
    "The check cannot prove what reaches the eardrum or distinguish every OS/driver/DAC fault by PCM alone.",
    "Complete the 60-second check, inspect challenge/digest/continuity, and enter an owner result.",
  ),
  reports: page(
    "Sessions & Reports",
    "Review immutable evidence, protocol phases, derived analysis, and exports.",
    "The main process redacts pre-reveal data, verifies hash chains/commitments, persists raw report locks and late annotations, and exposes versioned analysis.",
    "Inspect Integrity and protocol status first, then Overview/Timeline, Raw Report, Machine Output, Analysis, Audio & Configuration, and Files/Export. Reveal only after the gate is eligible.",
    "Do not read a post-hoc exploratory window as the primary endpoint, or treat participant return/report lock as evidence completion.",
    "Scheduled vs actual times, lateness/missed status, target-relative latency, primary/exploratory regions, integrity, reveal, and export hashes remain distinct.",
    "INCOMPLETE/RECOVERY_REQUIRED/TIMING_DEVIATION require review; REVEALED is owner-authorized, not a claim of causation.",
    "Reports do not prove paranormal effects, historical exactness, or analogue audio fidelity.",
    "Open a dry-run report, confirm pre-reveal redaction, lock, reveal after completion, verify integrity, and export.",
  ),
  aggregate: page(
    "Aggregate Workspace",
    "Analyze compatible revealed sessions without rewriting their evidence.",
    "The main process checks compatibility fingerprints and declared workflows before persisting an immutable cross-session analysis.",
    "Select compatible revealed sessions, review definitions and workflow, run the aggregate, then inspect its hash and exploratory/primary label.",
    "Do not mix incompatible profiles, use aggregate output to repair a failed session, or upgrade exploratory work after inspecting outcomes.",
    "Compatibility fingerprint, session IDs, workflow, analysis version, and integrity status define the aggregate.",
    "PASS means compatible immutable inputs and a persisted result; it does not establish a mechanism.",
    "Cross-session statistics cannot fix timing deviations or source-provenance uncertainty.",
    "Select at least two compatible revealed dry sessions and verify the persisted aggregate detail.",
  ),
  settings: page(
    "Settings & Data",
    "Inspect application identity, local data, backup/restore, import, export, and diagnostics.",
    "The page reads SQLite/schema/app/engine/audio/processor versions and invokes confirmed, hash-verified data operations in the main process.",
    "Review database path/size, create and verify backups, export diagnostics/session bundles, and use legacy import or restore only after confirmation and verification.",
    "Restore is risky/destructive to current data. Audio output label is only a human-readable label, not device selection. Do not delete evidence casually.",
    "Schema/version, database path/size, backup hash/status, import source status, export hashes, and output label have separate meanings.",
    "Verified backup/restore includes SQLite and evidence checks; warnings require owner review.",
    "Backup/restore does not prove scientific validity or physical audio fidelity.",
    "Create a verified backup, inspect its hash, restore in a test data directory, rerun integrity, and restart.",
  ),
});

export const FIELD_HELP = Object.freeze({
  experimentMode: "Research semantics: influence, future-target, control, or sham.",
  outcomeSpace: "The declared finite outcome domain; it may be symbolic rather than enumerated.",
  cardinality: "K, the exact number of possible outcomes.",
  rngProvider: "The configured authoritative random source; OS_CSPRNG is the operational default.",
  outputCadence: "The committed interval between machine opportunities.",
  targetAnchor: "The named event/time used as T for target-relative scheduling.",
  participantPhase: "The participant-facing interval; it is distinct from evidence monitoring.",
  evidencePhase: "The machine-evidence interval, which may continue after participant return.",
  prePostMonitoring: "Committed windows before and after T; late/early records do not rewrite the endpoint.",
  primaryEndpoint: "The declared scoring region or rule.",
  exploratoryWindow: "A declared secondary observation region, not a substitute primary endpoint.",
  profileVersion: "Immutable protocol identity used by a session.",
  recipeVersion: "Immutable synthesis identity used by a session.",
  provenance: "Source class for each material parameter; it is not a marketing claim.",
  sampleRate: "Digital PCM frames per second.",
  leftRightFrequency: "Explicit left and right oscillator frequencies in Hz.",
  binauralDifference: "Absolute right-minus-left component difference in Hz.",
  componentGain: "Amplitude multiplier for one carrier/component.",
  noiseGain: "Amplitude multiplier for generated noise only.",
  masterGain: "Top-level preview/audio amplitude stage, applied with a de-click ramp.",
  headroom: "dB attenuation reserved before PCM clipping.",
  am: "Declared amplitude modulation rate/depth/phase, or NONE.",
  fm: "Declared frequency modulation rate/deviation/phase, or NONE.",
  delay: "Declared delayed-sample mix/feedback stage, or NONE.",
  comb: "Declared comb/current-plus-delayed stage, or NONE.",
  sweep: "Declared low-frequency or delay sweep and phases, or NONE.",
  envelope: "Attack/decay/sustain/release amplitude shape.",
  audioHealth: "Digital telemetry plus a separate owner listening observation.",
  continuity: "Agreement between expected and actual AudioWorklet frame clocks.",
  pcmDigest: "SHA-256 over the canonical PCM16LE interleaved digital stream.",
  configFingerprint: "Hash of normalized synthesis material, excluding provenance presentation metadata.",
  integrityVerification: "Hash-chain, commitment, immutable-record, and version checks.",
  rawReportLock: "Immutable owner report record; drafts remain mutable until lock.",
  revealEligibility: "All declared evidence/protocol gates are complete and reveal is authorized.",
  backup: "Verified copy of local SQLite/evidence data.",
  restore: "Confirmed replacement/recovery from a verified backup.",
  export: "A hashed, portable report/evidence bundle; export does not mutate the source.",
});

export function getPageHelp(pageId) { return PAGE_HELP[pageId] || PAGE_HELP.start; }
export function getFieldHelp(fieldId) { return FIELD_HELP[fieldId] || null; }
